my files in my virtual environment folder:

asgiref==3.7.2  # happens automaticly
Django==4.2.4   # run 'py -m pip install django' in your terminal, after install run the prompt  
psycopg2==2.9.7 # pip install psycopg2
sqlparse==0.4.4 # happens automaticly
tzdata==2023.3  # happens automaticly